我想要做一个新的需求。
# 我需要把某个用户下，所有的持仓，当成一只基金来看，算出从起始日到今天，所有交易日的净值。从1开始。
# 数据库里要新增一张表记录这些数据。提供计算净值的接口，理论上我有所有的交易记录+出入金记录，是可以计算的。出入金本身也不影响净值，和基金一样。
# 其他现金收入，这个在数据库落数据的净值中，不纳入计算。前端页面显示净值的时候，用所有金额/所有交易日，算出线性的每天的收益，然后纳入净值。也就是数据库存的净值和实际显示的不一样。
# 前端页面要能够显示，所有时间段的净值曲线（引入echart），另外有，本年度，近一年，的快筛。暂时不做自定义时间段选择。
# 港股我觉得，如果是持仓阶段，可以简化下直接用今日的汇率算。如果是已经完成的交易，那就按照实际金额计算收益，纳入净值。
# 页面名称是，净值分析。除了图表，还需要有多个指标的计算，比如夏普比率，最大回撤，卡玛比率，年化收益（净值不受出入金影响，是否会和xirr不一样）
# 前端尽量复用现有控件。
# 前后端代码尽量复用现有能力和工具方法。
# 前端有手动刷新净值的方法，在admin里有操作的时候，提醒用户刷新净值。基于成本考虑，能否提供增量刷新和全量刷新能力（前提是两个计算成本差异很大）。

